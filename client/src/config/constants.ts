export const API_CONFIG = {
  BASE_URL: process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001/api",
  TIMEOUT: 10000,
};

export const APP_CONFIG = {
  NAME: "Padel Booking",
  VERSION: "1.0.0",
  DESCRIPTION: "Padel court booking system",
};
